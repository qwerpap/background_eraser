import 'package:flutter/material.dart';

class EraserScreen extends StatelessWidget {
  const EraserScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: const Center(
        child: Text('Eraser Screen'),
      ),
    );
  }
}

