# Background Eraser

Flutter приложение для удаления фона с изображений с использованием AI-технологий.

## 📱 Описание

Background Eraser — это мобильное приложение, которое позволяет быстро и легко удалять фон с фотографий. Приложение использует API от remove.bg для качественной обработки изображений и сохраняет результаты в локальной базе данных для быстрого доступа.

## ✨ Основные возможности

- 📸 **Загрузка изображений** — выбор фото из галереи или съемка новой
- 🎨 **Удаление фона** — автоматическое удаление фона с помощью AI
- 💾 **Локальное хранение** — сохранение обработанных изображений в SQLite базе данных
- 📚 **Галерея** — просмотр всех обработанных изображений
- ↩️ **Undo/Redo** — отмена и повтор действий при обработке
- 🗑️ **Очистка данных** — возможность удалить все сохраненные изображения

## 🏗️ Архитектура

Проект построен с использованием **Clean Architecture** и разделен на слои:

```
lib/
├── core/                    # Общая функциональность
│   ├── bloc/               # Регистрация зависимостей
│   ├── domain/             # Общие доменные сущности и утилиты
│   ├── navigation/         # Навигация приложения
│   ├── services/           # Сервисы (галерея, камера)
│   ├── shared/             # Переиспользуемые виджеты
│   └── theme/              # Темы и стили приложения
└── features/               # Фичи приложения
    ├── eraser/             # Удаление фона
    │   ├── data/           # Data слой (datasources, models, repositories)
    │   ├── domain/         # Domain слой (entities, repositories, usecases)
    │   ├── bloc/           # State management
    │   ├── view/           # UI экраны
    │   └── widgets/        # UI компоненты
    ├── home/               # Главный экран с галереей
    └── profile/            # Экран профиля и настроек
```

### Слои архитектуры

- **Presentation Layer** — UI компоненты, Bloc/Cubit для управления состоянием
- **Domain Layer** — бизнес-логика, entities, use cases, repository интерфейсы
- **Data Layer** — реализация репозиториев, datasources, DTO, база данных

## 🛠️ Технологии

### Основные зависимости

- **flutter_bloc** ^9.1.1 — State management
- **get_it** ^9.2.0 — Dependency Injection
- **go_router** ^17.0.1 — Навигация
- **dio** ^5.9.0 — HTTP клиент для API запросов
- **drift** ^2.14.1 — SQLite ORM для локальной базы данных
- **image_picker** ^1.2.1 — Выбор изображений из галереи/камеры
- **talker_flutter** ^5.1.9 — Логирование и отладка
- **permission_handler** ^12.0.1 — Управление разрешениями

### База данных

Используется **Drift** (SQLite) для локального хранения:
- Таблица `erased_images` — метаданные обработанных изображений
- Файлы изображений хранятся в файловой системе
- Автоматическая очистка несуществующих файлов

## 🚀 Установка и запуск

### Требования

- Flutter SDK >= 3.10.4
- Dart SDK >= 3.10.4
- Android Studio / Xcode (для iOS)

### Установка

1. Клонируйте репозиторий:
```bash
git clone <repository-url>
cd background_eraser
```

2. Установите зависимости:
```bash
flutter pub get
```

3. Сгенерируйте код для Drift:
```bash
flutter pub run build_runner build --delete-conflicting-outputs
```

4. Настройте API ключ (опционально):
Отредактируйте `lib/constants/api_constants.dart` и укажите свой API ключ для remove.bg

5. Запустите приложение:
```bash
flutter run
```

## 📦 Сборка

### Android
```bash
flutter build apk --release
```

### iOS
```bash
flutter build ios --release
```

## 🗂️ Структура проекта

### Features

#### Eraser Feature
Обработка изображений и удаление фона:
- **Remote DataSource** — интеграция с remove.bg API
- **Local DataSource** — работа с локальной базой данных
- **Use Cases**:
  - `RemoveBackgroundUseCase` — удаление фона
  - `SaveErasedImageUseCase` — сохранение обработанного изображения
  - `GetRecentErasedImagesUseCase` — получение списка изображений
  - `ClearAllErasedImagesUseCase` — очистка всех данных

#### Home Feature
Главный экран с галереей обработанных изображений:
- Отображение сетки изображений
- Загрузка новых фото
- Навигация к экрану обработки

#### Profile Feature
Экран настроек и информации:
- Информация о приложении
- Очистка всех сохраненных изображений

## 🔧 Настройка

### API конфигурация

API ключ для remove.bg настраивается в `lib/constants/api_constants.dart`:
```dart
static const String removeBgApiKey = 'YOUR_API_KEY';
```

### База данных

База данных автоматически создается при первом запуске в директории документов приложения. Версия схемы управляется через `schemaVersion` в `EraserDatabase`.

## 🧪 Разработка

### Генерация кода

После изменения схемы базы данных или моделей Drift:
```bash
flutter pub run build_runner watch --delete-conflicting-outputs
```

### Логирование

Приложение использует `Talker` для логирования. Логи доступны через DevTools или в консоли.

## 📝 Правила разработки

- Соблюдение Clean Architecture
- Все Bloc/Cubit должны быть зарегистрированы в `BlocProviders`
- Использование Either/Result для обработки ошибок
- DTO используются только в data слое
- Domain слой не зависит от Flutter/UI
- Все async операции должны быть правильно await'нуты

## 🤝 Вклад

1. Создайте feature branch (`git checkout -b feature/AmazingFeature`)
2. Зафиксируйте изменения (`git commit -m 'Add some AmazingFeature'`)
3. Запушьте в branch (`git push origin feature/AmazingFeature`)
4. Откройте Pull Request

## 📄 Лицензия

Этот проект является частной собственностью.

## 👨‍💻 Автор

Разработано для MST projects
